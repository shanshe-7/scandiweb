import React, { Component } from "react";

export default class Checkout extends Component {
  render() {
    return (
      <div style={{ height: "100vh", background: "rgba(57, 55, 72, 0.22)" }}>
        Checkout page
      </div>
    );
  }
}
